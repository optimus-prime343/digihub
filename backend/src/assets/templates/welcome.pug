extends base
block content
    p Hello #{fullName}
    p Welcome to Digihub, we are excited to have you onboard.
    p Please click the button below to verify your email address 
        table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
            tbody
                tr
                    td(align='left')
                        table(role='presentation' border='0' cellpadding='0' cellspacing='0')
                            tbody
                                tr
                                    td
                                        a(href=`http://localhost:4000/api/v1/auth/verify-account/${token}`, target='_blank') Verify Email Address
    if merchantMessage
        p #{merchantMessage}