export const getProductImageUrl = (imageName: string) => {
  return `http://localhost:4000/images/product-images/${imageName}`
}
export const getProfileImageUrl = (imageName: string) => {
  return `http://localhost:4000/images/profile-images/${imageName}`
}
